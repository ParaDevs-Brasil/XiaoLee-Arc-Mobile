# XiaoLee Backend — Dependências para Docker (produção)
# Diferença do requirements.txt: sem locust (apenas em dev/CI)

# ─── Core API ─────────────────────────────────────────────────────────────────
fastapi==0.136.0
uvicorn[standard]==0.45.0
uvloop==0.21.0
pydantic==2.13.3
pydantic-settings==2.14.0
python-multipart==0.0.20

# ─── Banco de Dados ───────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.41
aiosqlite==0.22.1
asyncpg==0.31.0
alembic==1.18.4

# ─── Redis ────────────────────────────────────────────────────────────────────
redis[asyncio]==7.4.0

# ─── Solana ───────────────────────────────────────────────────────────────────
solders==0.26.0
PyNaCl==1.6.2

# ─── Stellar ──────────────────────────────────────────────────────────────────
stellar-sdk>=10.0.0
PyJWT>=2.8.0

# ─── HTTP ─────────────────────────────────────────────────────────────────────
aiohttp==3.13.5
httpx==0.27.0

# ─── IA / LLM ─────────────────────────────────────────────────────────────────
anthropic>=0.69.0          # Claude (Messages API) — engine agêntica (LLM_PROVIDER=anthropic)

# ─── Integrações ──────────────────────────────────────────────────────────────
python-telegram-bot>=21.0

# ─── AI / Agent Engine (Sprint Lepton) ───────────────────────────────────────
anthropic>=0.111.0
openai>=1.0.0

# ─── Arc / Circle / CCTP (Sprint Lepton) ─────────────────────────────────────
web3>=6.20.0                # EVM on-chain: CCTP depositForBurn + receiveMessage
eth-account>=0.13.0         # key derivation + tx signing + verify_wallet_ownership (register)
eth-abi>=4.0.0              # ABI decode do evento MessageSent (CCTP raw_message extraction)
cryptography>=42.0.0        # RSA-OAEP do entitySecretCiphertext (Circle W3S developer wallets)

# ─── PQC — Post-Quantum Cryptography (Sprint Lepton) ─────────────────────────
dilithium-py>=1.0.0         # ML-DSA-87 (FIPS 204) — receipt signing pós-quântico

# ─── Utilitários ──────────────────────────────────────────────────────────────
python-dotenv==1.1.0
